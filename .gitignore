# dependencies
node_modules/

# next.js
.next/
out/
next-env.d.ts

# secrets (user-provided Firebase config + service account)
.env
.env.local
serviceAccount*.json

# dev fixture (regenerated by import pipeline)
data/normalized.json

# misc
*.log
.DS_Store

# raw source data + fixtures (contains worker PII — never publish)
data/
